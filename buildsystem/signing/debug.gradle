android {
    signingConfigs {
        debug {
            storeFile file('../buildsystem/signing/debug.keystore')
            storePassword 'android'
            keyAlias 'androiddebugkey'
            keyPassword 'android'
        }
    }
}